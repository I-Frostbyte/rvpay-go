# RVPay Go — Stage Checkpoint

## Stage: Agent 001 — First-Install OAuth + Client Resolution — COMPLETE
- [x] Stateless callback resolves via integration.external_account_id, falls back to payment_provider_configs.location_id
- [x] ProcessCallback reuses CREATED integration (activates) instead of ErrIntegrationAlreadyExists; ACTIVE still conflicts
- [x] INSTALL webhook dispatcher creates/finds payment_provider_configs idempotently; wired in main.go
- [x] Multiple-client safety: locationId → specific integration → client; missing mapping fails safely
- [x] Tests: oauth/webhooks/providers/http/cmd PASS; go vet PASS; gofmt clean

## BLOCKER: First-install mapping
- [x] Pre-provisioned integration has external_account_id="" (InstallIntegration). No safe GHL locationId → RVPay client mapping on brand-new install without OAuth completing first. Reported per agent 001.

## Next: Real HighLevel connectivity testing per clients/docs/connectivity-checklist.md